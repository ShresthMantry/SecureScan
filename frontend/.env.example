# API Base URL
# IMPORTANT: For physical device testing (Expo Go), replace localhost with your computer's IP address
# Find your IP with: ifconfig | grep "inet " | grep -v 127.0.0.1
# Example: EXPO_PUBLIC_API_URL=http://192.168.1.20:3000/api
# Note: Must use EXPO_PUBLIC_ prefix for Expo to read environment variables
EXPO_PUBLIC_API_URL=http://localhost:3000/api
